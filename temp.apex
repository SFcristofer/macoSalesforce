Map<String, Schema.SObjectField> fields = Schema.SObjectType.PermissionSet.fields.getMap(); for (String f : fields.keySet()) { if (f.containsIgnoreCase('twofactor') || f.containsIgnoreCase('mfa')) { System.debug('Field: ' + f); } }
