/**
 * Update balance in account
 */
trigger CarteraImporte on Cartera_Importe__c (after insert) {
    /*CarteraImporteTriggerHelper cith = null;

    if(trigger.isInsert) {
        cith = new CarteraImporteTriggerHelper(Trigger.new, CarteraImporteTriggerHelper.INS);
    }

    cith.execute();*/
}